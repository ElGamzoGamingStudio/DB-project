<Window x:Class="PathToSuccess.ScheduleTimeline"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ScheduleTimeline" Height="300" Width="1200">
    <Grid>
        <Grid Name="MainField">
            <ScrollViewer Name="ScrollMasta" HorizontalScrollBarVisibility="Visible">
                <StackPanel Name="TimelinePanel" Orientation="Horizontal" CanHorizontallyScroll="True">

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
