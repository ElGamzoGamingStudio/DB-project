<Window x:Class="PathToSuccess.ScheduleTimeline"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Расписание" Height="350" Width="1200" ResizeMode="NoResize">
    <Grid>
        <Grid Name="MainField">
            <ScrollViewer Name="ScrollMasta" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Auto">
                <StackPanel Name="TimelinePanel" Orientation="Horizontal" CanHorizontallyScroll="True">

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
